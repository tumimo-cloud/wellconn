# WellnessConnect — Web Build (v3)

Fixes in this build:
- v3: Ionicons font now preloaded via `useFonts` — icons render correctly on web
- v2: OAuth callback race condition fixed

Deploy: drag this folder onto Netlify Deploys. SPA fallback pre-configured.
